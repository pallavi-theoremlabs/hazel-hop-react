# Deployment artifact for the Azure App Service tier. Exact pins for the same
# reason as the root requirements.txt: a `>=` here is a future AttributeError in a
# container nobody is watching.
#
# Versions match the backend's where the package is shared, so one venv can run
# both locally.

fastapi==0.116.1
uvicorn==0.33.0
httpx==0.28.1

# Transitive closure.
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
colorama==0.4.6
h11==0.16.0
httpcore==1.0.9
idna==3.18
pydantic==2.13.4
pydantic-core==2.46.4
sniffio==1.3.1
starlette==0.47.3
typing-extensions==4.16.0
typing-inspection==0.4.4
